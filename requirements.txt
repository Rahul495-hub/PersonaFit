Flask>=3.0
numpy>=1.26
pandas>=2.2
scikit-learn>=1.4
joblib>=1.3

# Used by data_augmentation.py for SMOTE-based dataset augmentation.
# The script falls back to a simple bootstrap resample if this isn't
# installed, so it's optional but recommended for genuine SMOTE output.
imbalanced-learn>=0.12

# Used by eda.py to generate exploratory data analysis charts/reports.
matplotlib>=3.8
seaborn>=0.13
scipy>=1.11

# Optional: only needed if ANTHROPIC_API_KEY is set, to enable the LLM
# generation step in rag.py. The app works fully offline without it.
# anthropic>=0.40 